<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag 'navbar', media: 'all', 'data-turbolinks-track': 'reload' %>
  <title>Jimmy</title>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #F3DBAE;">
    <div class="container">
      <!-- Back button with an image and text -->
      <%= link_to new_activity_preference_path, class: "navbar-brand" do %>
        <%= image_tag('back_arrow.png', alt: 'Back', class: 'img-fluid', style: 'height: 24px;') %>
        <span class="ml-1">Back</span>
      <% end %>
      <h1 class="navbar-text mx-auto my-auto var(--primary-text-color)">Experience Level</h1>
      <%# Centering the title with identical content on the right %>
      <div class="navbar-brand">
        <span class="right-placeholder mr-1"> Back </span>
         <%= image_tag('back_arrow.png', alt: 'Back', class: 'hidden-arrow', style: 'height: 24px;') %>
      </div>
    </div>
  </nav>

  <% @activity=Activity.find(params[:id])%>
  <h1 class="workoutpref-landing-title"> Select your experience level for <%=@activity.activity_name%>... </h1>
  <div class="experience-select-div">

    <%= form_for @new_pref do |form| %>
      <div class="experience-select-btn">
        <% @levels.each do |level| %>
          <div class="radio-label-pair">
            <%= form.radio_button :experience_level, level, class: "bullet" %>
            <%= form.label level, level %>
          </div>
        <% end %>
      </div>
      
      <%= form.hidden_field :activity_id, value: @activity.id %> 
      
      <%= form.submit 'Save', class: 'edit-save-btn' %>
    <% end %>

  


  </div>

</body>