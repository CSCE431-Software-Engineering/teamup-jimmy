<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag 'navbar', media: 'all', 'data-turbolinks-track': 'reload' %>
  <title>Jimmy</title>
</head>
<body>
  <%= render 'pages/navbar' %>
  <!-- logo -->
  <!-- intro -->
  <div class="home-title">
    <p>You're signed in!</p>
    <p>Here are your matches.</p>
  </div>
  <br/>
  <div class="matches-container">
    <% @matches.each do |match| %>
      <div class="match">
        <% matched_student = Student.find_by(email: match.student2_email) %>
        <p>You matched with <%= matched_student.name %>!</p>
      </div>
    <% end %>
  </div>
</body>
</html>
