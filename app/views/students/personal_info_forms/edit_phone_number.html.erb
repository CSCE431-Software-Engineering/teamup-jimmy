<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Jimmy</title>
</head>
<body>
<nav class="material-header">
  <div class="material-banner">
    <%= link_to students_personal_info_path,  class: "navbar-link material-back-button" do %>
      <%= image_tag('back_arrow.png', alt: 'Back', class: 'back-arrow-icon') %> Back
    <% end %>
    <h1 class="material-title">Edit Phone Number</h1>
    <span class="material-spacer"></span>
  </div>
</nav>
  <div class="edit-profile-main">
    <div class="edit-profile-inner">      
      <%= form_with(model: @current_student, local: true, method: :patch) do |form| %>
        <table class="students-edit-info-form">
          <tr class="students-edit-info-form-row">
            <th>
              <div class="edit-form-field">
                <p class = "edit-form-label">New phone number</p>
                <p class = "edit-form-label edit-form-subscript">Do not include spaces, parentheses, or dashes.</p>
                <%= form.text_field :phone_number, placeholder: 'Phone number', class: 'custom-styled-input', pattern: "^[0-9]{10}$", title: "Please enter a 10-digit phone number without spaces, parentheses, or dashes." %>
              </div>
            </th>
          </tr>
        </table>
        <div class="submit-button-container">
          <%= form.submit "Save", class: "material-button" %>
        </div>
      <% end %>  
      <%= render 'pages/flash' %>
    </div>
  </div>    

  </body>
</html>