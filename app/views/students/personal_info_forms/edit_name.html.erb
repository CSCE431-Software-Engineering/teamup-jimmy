<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Jimmy</title>
</head>
<body>
  <%= render 'pages/navbar_inner_page' %>
  <div class="general-div-main">
    <div class="general-div-inner">      
      <%= form_with(model: @current_student, local: true) do |form| %>
        <table class="students-edit-info-form">
          <tr class="students-edit-info-form-row">
            <th>
              <div class="edit-form-field">
                <p class = "edit-form-label">New display name</p>
                <%= form.text_field :name, placeholder: 'First Last', class: 'custom-styled-input' %>
              </div>
            </th>
          </tr>
        </table>
        <div class="submit-button-container">
          <%= form.submit "Save", class: "material-button" %>
        </div>
      <% end %>  
      <%= render 'pages/flash' %>
    </div>
  </div>    

  </body>
  <%= render 'landing/footer' %>
</html>