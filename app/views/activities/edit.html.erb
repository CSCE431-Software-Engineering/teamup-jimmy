<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag 'navbar', media: 'all', 'data-turbolinks-track': 'reload' %>
  <title>Jimmy</title>
</head>

<body>
  <%= render(partial:'back', locals:{c: "activities", a: "new", s: "edit-back-btn"}) %>

  <h1 class="activity-landing-title"> Select your experience level for <%=@activity.activity_name%>... </h1>
  <div class="experience-select-div">
    <%= form_with model: @model do |form| %>
      <% @levels.each do |level| %>
      <div class="experience-select-btn">
        <%= form.radio_button :level, level; %>
        <%# the first level is attribute name %>
        <%= form.label level, level %><br>
      </div>
      <% end %>
  
    <%= form.submit class: 'edit-save-btn' %>
  <% end %>
</div>

</body>